language: node_js

sudo: false

cache:
  apt: true
  directories:
    - node_modules

node_js:
  - "8"

before_script:
  - git config --global user.email "hi@abnerchou.me"
  - git config --global user.name "Abner Chou"

script:
  - npm start

after_script:
  - echo "Completed."
